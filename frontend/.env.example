# URL base da API do backend (injetada em runtime, nunca no build).
# Em SSR o servidor Nuxt pode acessar o backend pela rede interna, enquanto o
# browser usa a URL pública — podem ser valores distintos.
NUXT_PUBLIC_API_BASE=http://localhost:3001
